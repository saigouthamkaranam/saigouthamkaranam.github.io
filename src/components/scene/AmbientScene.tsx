// Intentionally empty - all amateur 3D visuals removed
// The only 3D element is the premium CinematicMonolith in ProductScene

export default function AmbientScene() {
  // Apple-style: no decorative 3D elements
  // Only the hero monolith exists
  return null;
}
